/**
 * This package is a SuperNative EDGE UI component. There are no native
 * bridge functions to call from Vue, React, or Livewire JavaScript.
 *
 * Use `<native:wheel-date-picker>` in a NativeComponent Blade view, then
 * rebuild with `php artisan native:run`.
 */
export {};
